spring:
  application:
    name: demographql
  graphql:
    graphiql:
      enabled: true
  datasource:
    url: jdbc:mysql://localhost:3306/oraganization
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 9090

